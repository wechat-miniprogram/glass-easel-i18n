{
  "scripts": {
    "init-dev": "pnpm i && npm run build",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^5.2.2",
    "webpack": "^5.85.0",
    "webpack-cli": "^5.0.1"
  }
}
